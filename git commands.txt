git branch = show branches
git branch -c suraj = it creates a new branch ( -c means create) 
git checkout suraj = it switches from main branch to suraj branch 
git status = shows you which files are remaining to upload on main branch
git add index.html = it add files to main branch
git commit -m = it use for telling what have you done 
git checkout origin head = push your changes to git hub
 